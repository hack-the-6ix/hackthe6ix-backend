export const fetchUserInfo = async (email: String) => {
    // Search in users
    // Search in external users
    
    // Check for suffix field
    // Check for roles

    /*{
        firstname,
        lastname,
        email,
        roles,
        suffix
    }
    */
}

export const associateUser = async (email: String, discordID: String) => {
    // Search in users
    // Search in external users

    // Set discordID field
}