export enum Templates {
  applicationIncomplete = 'applicationIncomplete',
  applied = 'applied',
  confirmed = 'confirmed',
  declined = 'declined',
  accepted = 'accepted',
  rejected = 'rejected',
  waitlisted = 'waitlisted'
}

export enum Lists {
  applicationIncomplete = 'applicationIncomplete',
  applied = 'applied',
  confirmed = 'confirmed',
  declined = 'declined',
  accepted = 'accepted',
  rejected = 'rejected',
  waitlisted = 'waitlisted'
}
